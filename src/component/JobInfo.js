import Wrapper from "../assets/wrappers/JobInfo";

const JobInfo = ({ icon }) => {
    return (
        <Wrapper>
            <span className="icon">{icon}</span>
        </Wrapper>
    )
}

export default JobInfo